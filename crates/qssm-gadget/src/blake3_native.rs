//! Phase 2 (planned): BLAKE3 quarter‑round / Merkle‑parent circuit using only `bits` primitives.
//!
//! Implement G‑function and fixed 64‑byte `hash_domain` preimage hashing here; not yet required for
//! Merkle `recompute_root` (native `blake3` via `qssm-utils`).

#![allow(dead_code)]
#![allow(missing_docs)]
