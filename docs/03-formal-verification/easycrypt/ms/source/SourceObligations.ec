(* MS-3a source obligations: programmed seed fields, paired public fields, schedule
   axiom, and constructor packaging (split modules). *)
require export SourceProgrammedObligations.
require export SourcePublicFieldObligations.
require export SourceScheduleObligations.
