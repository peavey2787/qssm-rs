(* Stable facade for the MS game-hop chain. It groups hop types, single-step
	transition bounds, and the composed MS-hop lemmas behind one import. *)
require import GameMSHopTypes GameMSHopTransitions GameMSHopComposition.
