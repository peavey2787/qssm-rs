(* Stable facade for the MS-3a obligation surface. It bundles programmed seed
   facts, paired public-field facts, and the schedule interface. *)
require export SourceProgrammedObligations.
require export SourcePublicFieldObligations.
require export SourceScheduleObligations.
